[project]
name = "stc-ml-test"
version = "0.1.0"
description = ""
authors = [
    {name = "Matthew Whalen",email = "mwhalen@ryanrts.com"}
]

requires-python = ">=3.11,<4.0"
dependencies = [
    "pandas (>=3.0.3,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.3,<10.0.0)",
]
